
# - name: Get release name
#   debug: var=release.release_name

- name: Loop on the list of binaries for this release
  include_tasks: download_upload_binary.yml
  loop: "{{ release.binaries }}"
  loop_control:
    loop_var: binary
  